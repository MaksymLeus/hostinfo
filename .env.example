# ---------------------------------------------------------------------------
# Hostinfo Service Configuration
# ---------------------------------------------------------------------------

# Port the service should listen on inside the container (default 8080)
HOSTINFO_PORT=8080

# Interface or address to bind the HTTP server to
# 0.0.0.0 = allow external connections (Docker typical)
# 127.0.0.1 = local-only connections
HOSTINFO_ADDR=0.0.0.0

# Enable or disable debug/verbose mode
# Options: true / false
HOSTINFO_DEBUG=false
