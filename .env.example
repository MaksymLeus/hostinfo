# ---------------------------------------------------------------------------
# Hostinfo Service Configuration
# ---------------------------------------------------------------------------

# Port the service should listen on inside the container (default 8080)
HOSTINFO_PORT=8080

# Interface or address to bind the HTTP server to
# 0.0.0.0 = allow external connections (Docker typical)
# 127.0.0.1 = local-only connections
HOSTINFO_ADDR=127.0.0.1

# Enable or disable debug/verbose mode
# Options: true / false
HOSTINFO_DEBUG=false

# CORS Allowed Origins (comma-separated list)
# Example: http://localhost:5173,http://
# If not set, defaults to http://localhost:5173 and http://127.0.0.1:5173
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Frontend should be served separately or from embedded files
# If true, the server will not serve the frontend from embedded files and expects it to be served separately (e.g., via a reverse proxy or separate static server).
RUN_API_ONLY=false

# Path to the frontend build directory (used in development)
HOSTINFO_FRONTEND_PATH=./frontend/dist